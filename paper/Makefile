all:
	xelatex paper.tex
	bibtex paper.aux
	xelatex paper.tex
	xelatex paper.tex

single:
	xelatex paper.tex

clean:
	rm paper.aux
	rm paper.bbl
	rm paper.blg
	rm paper.log
	rm paper.nav
	rm paper.out
	rm paper.snm
	rm paper.toc
